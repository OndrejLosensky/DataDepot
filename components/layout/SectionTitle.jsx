import React from 'react'

const SectionTitle = ({name}) => {
  return (
    <div className='text-2xl text-center pb-1 font-thin text-blue-500'>
        <p>{name}</p>
    </div>
  )
}

export default SectionTitle